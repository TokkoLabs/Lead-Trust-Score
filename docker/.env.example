# Copia a .env en la raiz del repo.
# GITHUB_EXPECTED_USER: login de GitHub esperado (opcional, validado por init)
GITHUB_EXPECTED_USER=

# APP_PORT: puerto host para el servicio app (perfil product)
APP_PORT=8080
